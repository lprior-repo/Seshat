STATUS: REFACTORED
Split fps.rs into fps/mod.rs, fps/report.rs, and fps/measurement.rs.

Refactored `diagram_tool/src/geometry/snap/grid_snap.rs` to extract tests into `diagram_tool/src/geometry/snap/grid_snap_tests.rs`. Both files are now well under the 300 line limit (70 lines and 246 lines respectively). Applied Scott Wlaschin DDD principles and the "Code is a Liability" mindset by making the code strictly separated and DRY. Tests passed cleanly and the target module is free of clippy warnings.

Refactored `diagram_tool/src/export/png.rs`. Reduced from 318 lines to 211 lines (well under the 300-line limit). Applied EXTREMELY DRY test utilities (e.g. `create_doc`, `export_to_temp`) condensing repetitive test setups. Applied Scott Wlaschin DDD principles by replacing the primitive path `&str` with `impl AsRef<std::path::Path>` for the `export_png` path argument, removing primitive obsession. Ensures valid domains without redundant boilerplate.

Refactored `diagram_tool/src/viewport/operations.rs`. Reduced from 327 lines to 110 lines. Applied "Code is a Liability" and EXTREMELY DRY principles by entirely eliminating dead/redundant wrapper methods (`apply_pan`, `apply_zoom_in`, etc.) that were merely polluting the imperative shell and proxying directly to existing methods on `ViewportState`. This significantly improved cohesion and tightened the domain boundary by relying directly on the pure core logic within `ViewportState` instead. Removed redundant test cases mirroring `viewport.pan` tests. The module is free of clippy warnings and well under the 300 line limit.

Refactored `diagram_tool/src/export/svg_builder/edges.rs` to strictly < 300 lines (currently ~150). Applied EXTREMELY DRY constraints to the test suite, extracting 14 identical boilerplate setups into a single parameterized `verify_node(...)` helper that returns `Result<(), TestCaseError>`. Eliminated all clippy warnings including `unwrap_used` by employing `?` in tests. Applied Scott Wlaschin DDD principles by keeping the logic type-driven.

Refactored `diagram_tool/src/cli.rs` which was ~290 lines. Created a new module `diagram_tool/src/cli/commands.rs` strictly to hold the `Commands` enum, argument parsing boundaries, and execution mapping. Applied DDD by encapsulating the command `execute` behavior and `name` generation directly on the `Commands` type itself. `cli.rs` is now highly DRY, acts purely as an imperative orchestration shell with fewer than 50 lines, and relies on strict type transitions. Compiles cleanly with no clippy warnings.