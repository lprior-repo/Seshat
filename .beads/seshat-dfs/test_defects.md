# Test Defects - Bead seshat-dfs

## STATUS: REJECTED

## Critical Defects

### DEFECT-001: No Executable Test Code
**Severity**: CRITICAL  
**Category**: Testing Trophy Violation

**Description**: The bead contains only markdown specification documents (`contract.md` and `martin-fowler-tests.md`) with zero actual Rust test code. The martin-fowler-tests.md describes test INTENT but provides no executable tests.

**Evidence**:
- No `.rs` files in `.beads/seshat-dfs/`
- martin-fowler-tests.md is 242 lines of markdown specifications
- No `#[test]` functions exist
- Cannot run `cargo test` on this bead

**Required Fix**: Implement actual test code in Rust that exercises:
1. Unit tests for `handle_canvas_double_click_node_creation`
2. Unit tests for `handle_toolbar_add_node`  
3. Unit tests for `create_node_add_envelope`
4. Integration tests for dispatch flow with mock db_tx

---

### DEFECT-002: Missing BDD Given-When-Then Execution
**Severity**: CRITICAL  
**Category**: Dan North BDD Violation

**Description**: The BDD scenarios in martin-fowler-tests.md are documented but not executable. BDD requires tests to be executable specifications that can fail when behavior deviates.

**Evidence**:
- Test names follow GWT format but are not `#[test]` functions
- No test harness to execute scenarios
- No assertions (the markdown uses "Then:" as prose, not actual `assert!` macros)

**Required Fix**: Write actual Rust tests that:
1. Use `#[test]` attribute
2. Include proper Given-When-Then in comments (good)
3. Execute assertions with `assert_eq!`, `assert!`, etc.

---

### DEFECT-003: No ATDD DSL Implementation
**Severity**: HIGH  
**Category**: Dave Farley ATDD Violation  

**Description**: The contract.md defines WHAT (preconditions, postconditions, invariants) but there's no DSL or test helper layer to express these as executable specifications.

**Evidence**:
- No builder/helper types for creating test fixtures
- No DSL for expressing domain operations in tests
- No separation between test intent and implementation details

**Required Fix**: Implement test helpers:
```rust
// Example DSL pattern needed
fn given_empty_canvas() -> TestCanvas { ... }
fn when_user_double_clicks(canvas: &TestCanvas, at: (f64, f64)) -> TestResult { ... }
fn then_node_exists(doc: &DiagramDocument, at: (f64, f64)) { ... }
```

---

### DEFECT-004: No Test Isolation Verification
**Severity**: MEDIUM  
**Category**: Kent Beck TDD Violation

**Description**: Cannot verify tests are isolated, fast, or deterministic without actual test code. Each test should be independent with no shared mutable state.

**Required Fix**: Write tests that:
1. Create fresh document/state per test
2. Don't depend on execution order
3. Complete in milliseconds

---

### DEFECT-005: Missing Combinatorial Coverage  
**Severity**: MEDIUM  
**Category**: Test Completeness

**Description**: While the test plan lists happy path, error path, and edge cases, without code we cannot verify exhaustive coverage of:
- Invalid coordinates: NaN, Infinity, negative
- Invalid dimensions: 0, negative
- Tool mode variants: Select vs Pan vs Connect
- WAL state: Some vs None
- Hit test variants: node, edge, nothing

**Required Fix**: Implement parameterized tests or explicit test cases covering all variants.

---

## Summary

| Defect | Severity | Doctrine Violated |
|--------|----------|-------------------|
| DEFECT-001 | CRITICAL | Testing Trophy - No real execution |
| DEFECT-002 | CRITICAL | Dan North BDD - Not executable |
| DEFECT-003 | HIGH | Dave Farley ATDD - No DSL |
| DEFECT-004 | MEDIUM | Kent Beck TDD - No isolation proof |
| DEFECT-005 | MEDIUM | Combinatorial Exhaustiveness |

## Resolution Path

1. Create `diagram_tool/src/ui/dispatch/node_tests.rs` (or similar)
2. Implement unit tests for each function in the contract
3. Use proper assertions that can fail/pass
4. Ensure tests run with `cargo test`
5. Verify all preconditions (P1-P5), postconditions (Q1-Q6), and invariants (I1-I4) are tested

---

*Generated by Test Reviewer - Session 2026-03-12*
