--- seshat_cli/src/lib.rs
+++ replace get_help -> String with String::new()
@@ -145,18 +145,17 @@
             Err(Error::CommandExecution(ExecutionError::SimulatedFailure))
         }
         Cli::Run(_) | Cli::Help | Cli::Version => Ok(()),
     }
 }
 
 #[must_use]
 pub fn get_help() -> String {
-    use clap::CommandFactory;
-    ClapCli::command().render_help().to_string()
+    String::new() /* ~ changed by cargo-mutants ~ */
 }
 
 #[must_use]
 pub fn get_version() -> String {
     use clap::CommandFactory;
     ClapCli::command().render_version()
 }
 
