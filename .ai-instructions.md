# Agent Instructions

This repository uses Moon for build orchestration.

## Zero-Policy

- No `.unwrap()`, `.expect()`, `panic!()`, `todo!()`, `unimplemented!()`
- All errors use `Result<T, Error>` with proper propagation
